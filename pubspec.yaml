name: kelas_rapi
description: "Aplikasi jadwal dan piket kelas XI RPL B - SMK Negeri 3 Palu"
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.3.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.6
  flutter_local_notifications: ^17.2.3
  shared_preferences: ^2.2.3
  table_calendar: ^3.1.2
  timezone: ^0.9.4
  intl: ^0.19.0
  provider: ^6.1.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0

flutter:
  uses-material-design: true
